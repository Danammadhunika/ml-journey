# --- Web / API framework (used from v3 onward for the dashboard) ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.31.1,<1.0.0

# --- Data validation & settings ---
pydantic>=2.11.0,<3.0.0
pydantic[email]>=2.11.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# --- CLI ---
typer==0.12.5
click<8.2  # typer 0.12.x's --help rendering breaks on click>=8.2 (make_metavar signature change)
rich==13.8.1

# --- Database ---
sqlalchemy==2.0.35

# --- Data handling ---
pandas==2.2.3
openpyxl==3.1.5   # Excel (.xlsx) read/write, used by pandas for export-tracker

# --- PDF export (resume / cover letter as PDF) ---
reportlab==4.4.10

# --- HTTP clients ---
requests==2.32.3
httpx==0.27.2

# --- Templating (resume/cover-letter/email templates) ---
jinja2==3.1.4

# --- LLM provider ---
anthropic==0.34.2
tenacity==9.0.0    # retry/backoff for transient LLM API errors (rate limits, timeouts)

# --- Config / secrets ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.3
